-- 提供两个版本windows下的sparse.exe，和linux下的sparse。--


注意事项：

0.程序里没有做过多的差错控制，所以任何情况都可能崩溃，不要激动，这很正常！

1.sparse.exe和config.txt一定要放在一个目录下，不要改名字。

2.config.txt用来配置S口命令的结构，按照事例来写，一定要严格注意格式。全大写字母为关键字，不要更改。有些命令不需要INNERHEAD，不要写。

3.如果程序崩溃了，查看一下死前打印出的信息，看一下是否S口消息和代码中的构造有偏差。

4.最后，源文件也顺便带上，大家觉得有用的话，可以修改出更好的版本。

